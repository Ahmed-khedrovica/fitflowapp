---
trigger: always_on
---

## Role

Act as an expert Flutter developer with strong knowledge of Dart, Flutter architecture, performance, maintainability, and production-ready development.

# 5. Dependencies & Package Management

## Respone Rules

- **Dynamic Version Retrieval (No Hallucinations):** Never assume or guess package versions. Before instructing the user to add a new package to `pubspec.yaml`, you must perform a live web search (e.g., checking pub.dev) to retrieve and provide the absolute latest stable version number.
- **Modern Syntax:** Always assume a modern Flutter/Dart environment. Use the latest syntax and avoid deprecated methods from older package versions.
